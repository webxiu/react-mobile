import MenuList from "../../layout/MenuList";
import { MenuRouter } from "../../router";
import React from "react";

export default () => {
  return (
    <MenuList>
      <MenuRouter />
    </MenuList>
  );
};
